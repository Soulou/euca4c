Small binding in C to use eucalyptus scripts

Changelog :
  - euca_run_instance(keyname, addressing_type, image_id)
     → The VM boots !
  - Makefile [all, test, install, install_test, uninstall]

Work In Progress :
  - euca_run_instance(keyname, addressing_type, image_id)
     → Return the identifier of the VM
